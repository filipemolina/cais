/reference/
/dist/

# Compiled binary from `go build .` in the repo root. `make build` installs
# to $(go env GOPATH)/bin instead, so nothing here needs to be tracked.
/cais

# Screenshots and mock data
mocks/

# VHS byproduct (only the .png screenshots are used)
demo/screenshots.gif

# Backup store sidecar created by the running app next to a compose file.
# Test runs that resolve a compose inside the repo can create one too, so
# ignore it the same way the app's own .cais/.gitignore does.
/.cais/
/src/model/.cais/

# BEGIN oh-my-opencode-slim worktrees
.slim/worktrees/
.slim/worktrees.json
# END oh-my-opencode-slim worktrees
